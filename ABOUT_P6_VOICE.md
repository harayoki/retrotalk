# PC6000シリーズのボイスについて

Retro Talk の目的は、オリジナルのチープな音声合成を JavaScript で再現する事だが、
最初は PC-6001mkII / PC-6601 （以下 P6）のボイスをどこまで JS で再現できるかの調査と実験で始まった。
（そのため実機と同じ音声を再生したい、という人はエミュレータ系を当たってほしい。いいものがあるはず。）
このドキュメントは P6 の音声合成を、再現するために何をすればいいかの調査と実装の過程をまとめたものである。

## μPD7752(LSI) が何をしていたか

μPD7752 は P6 が積んでいた音声合成 LSI。声の「たどたどしさ」は、粗い音質が原因ではない。
10ms ごとにしか値を更新できず、1 度に動かせる幅が決まっている、というパラメータ側の窮屈さから出ている。

μPD7752 は PARCOR(偏自己相関)方式ではない。
NEC 技報の論文タイトルがそのまま `「ホルマントをパラメータとする音声合成LSI」` で、
外から与えるのは `フォルマント周波数と帯域幅`。
PARCOR は同時期の NEC の別系統で、隣の型番 μPD7751 は ADPCM。

粗さの出どころはサンプルレートでもビット深度でもない。

実機の内部は '約 9943Hz / 14 ビット' 。低くも粗くもない。
(設計は 10kHz だが、P6 のクロックが 3.579545MHz なので 3.6MHz 想定から間延びしている。
10000 × 3.579545 / 3.6 = 9943。音程にして約 10 セントの差になる)

粗いのはパラメータの方で — 10ms のフレーム、7 ビットのフォルマント番地、
4 ビットの音量、そして差分の幅。ここを詰めるほうが、量子化ノイズを足すより近づく。

## LSI は  音声再生の担当のみ 他は全てソフトウェア制御

全体を理解する鍵がここにある。規則音声合成とは言うが、チップがやるのは発声器官の再現だけで、
「ローマ字をどう音素に割り、フォルマントをどう動かすか」という実装は入っていない。

```
"ohayo-."
  ↓  BASIC ROM + 音声合成ROM のソフトウェア処理 ← ここが重い(Z80 4MHz 弱)
音素の並び
  ↓  フォルマントのフレーム列に展開(作業領域 0000h-3FFFh, 16KB)
7 バイト × N のフレーム
  ↓  ポート E0h へ 1 バイトずつ手渡し
μPD7752 = インパルス / ノイズ → 2 次 IIR × 5 段 → D/A
```

当時はメモリも CPU も弱かったので、できるだけ少ないデータ量で音声を表そうとした。
そのしわ寄せが、後で出てくるデーターの更新が '差分でしか指定できない' ＝ '1 回に動かせる幅が狭い' に出ている。


## LSI の中身

音源 (有声 = 一定周期のインパルス列 / 無声 = ノイズ)を、10 ビット係数の 2 次 IIR フィルタ 5 段に通すだけ。
5 本のフォルマント周波数と、それぞれの強度(帯域幅)を指定できる。

## 送るデータ — 1 フレーム 7 バイト

| バイト | bit7 | bit6 | bit5 | bit4 | bit3 | bit2 | bit1 | bit0 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | TIME | TIME | TIME | TIME | TIME | QMAG | S.I | V/UV |
| 2〜6 | f1〜f5 (5 ビット, −16〜+15) | | | | | b1〜b5 (3 ビット, −4〜+3) | | |
| 7〜 | AMP (4 ビット) | | | | FV | p (3 ビット, −4〜+3) | | |

| | |
| --- | --- |
| `TIME` | 最後の AMP/FV/p バイトを繰り返す回数。フレームを引き伸ばせる |
| `QMAG` | 1 で f1〜f5 と b1〜b5 が 2 倍になる |
| `S.I` | 1 でフレーム間の補間が効く。これがクセがでる原因 |
| `V/UV` | 1 で有声、0 で無声 |
| `FV` | 1 かつ有声で、有声摩擦音 |
| `AMP` | 音の大きさ。ここだけ絶対値で、おおむね指数関数的に効く |

f・b・p は全部「差分」で、チップの中で累積される。
データ量を削るための設計だが、副作用として一度ずれると後ろが全部ずれる。
ポート E2h で速度を設定し直すと、ピッチの累積はリセットされる。

(1 バイト目のビット配置は、実測データ `09h` `0Dh` `A0h` から逆算したもの。
表として明記された資料は見つけられなかった)

### ポート

| ポート | 向き | 中身 |
| --- | --- | --- |
| E0h | 読み | ステータス。bit7 BSY / bit6 REQ / bit5 INT/EXT / bit4 ERR |
| E0h | 書き | 上の 7 バイトのデータ |
| E1h | — | 使えない |
| E2h | 書き | bit2 = フレーム周期(0 で 10ms, 1 で 20ms)、bit1-0 = 速度(00 標準 / 01 遅い ×1.2 / 10 速い ×0.8 / 11 停止) |
| E3h | 書き | 00h〜04h = 固定語、FEh = 外からデータを送る、FFh = 発声停止 |

固定語はチップ内蔵で 0〜63 の枠があるが、P6 では 0〜4 しか入っていない
(「終わりました」「お待ち下さい」「故障中です」「いらっしゃいませ」「ありがとうございました」)。

この 5 つだけ、声が急に普通になった（という記憶が著者にある）。
あらかじめサンプリングされた音声のように聞こえた。
資料では固定語も同じチップで鳴らすことになっているので 裏は取れていない。

## ピッチのからくり：クセの原因 (1)

インパルスの間隔は、サンプリング周期(約 100.6μs)の整数倍 P で決まる。
P は 1〜128 で循環し、初期値は 30(≒3.0ms)。`p` は 1 フレームあたり ±4 の差分。

初期値 30 は「喋る声の高さ」ではない。
資料の言葉は「デフォルトのピッチ＋累積ピッチの合計を P と呼ぶ」で、30 は
電源を入れた直後の値にすぎないようだ。
音声データは喋りはじめる前に `p` を送ってそこから動かしていて、男声の例(`talk"m2 a."`)では `p=+3` を「必要回数繰り返す」
とある。`p` は周期を伸ばす向きなので、実際の声は 331Hz より低いところにいる。

実際の高さは資料に無い。一度ここを取り違えて既定を 331Hz にしたが、
耳で聞いて調整するかぎり P=38〜40(260〜250Hz)あたりが正解と思えた。

周波数ではなく周期が量子化されている。

| 高さ | P | 隣の P との差 |
| --- | --- | --- |
| 約 78Hz | 128 | 13.5 セント |
| 約 120Hz | 83 | 20.7 セント |
| 約 250Hz | 40 | 44 セント |
| 約 331Hz(初期値) | 30 | 57 セント |

高くなるほど音痴になる。 PC-6601 の歌う機能でまともに歌えるのが O3 の範囲だけなのはこれが理由。

周波数を等間隔で丸めると逆の特性(低音ほど粗い)になってしまう。丸めるのは周期のほう。


## フォルマントのからくり

`f` は Hz ではなく、チップ内部 ROM の「ホルマント周波数の収納番地」への差分。
7 ビット幅で 128 周期に循環する。実測すると、番地に対して周波数は指数関数的に動く
— つまり対数ではほぼ等間隔で、平均律の刻みに近い。

そして 5 本は同じテーブルを共有していて、既定の持ち場だけが違う。
F2 を基準にしたずれは以下。

| F1 | F2 | F3 | F4 | F5 |
| --- | --- | --- | --- | --- |
| +62 | 0 | +57 | +47 | +32 |

`B` (帯域幅)は 6 ビット幅あるが、実際に使えるのは 16 段階ほど。値を上げると強度が下がる。
既定強度では F2 だけが突出して強く、他はほとんど波形に効かない。
BASIC が F2 / F4 / F5 の 3 本だけで音を作っているのはそのため。


## 差分の幅：クセの原因(2)

1 フレーム(10ms)あたりに動かせる幅は、f が ±16 番地、b が ±4、p が ±4。

音素が切り替わっても、フォルマントは決まった速さでしか変化できない。
互換 BASIC の作者は、大抵の子音は開始時の周波数だけを持ち、
そこから接続する母音まで 最速で機械的に変化させている と書いている。

つまり繋ぎ目は切れているのではない。常に滑っている。ただし 10ms の階段で。
クロスフェードを外す・無音を挟む、という方向とは違う方向で
上限つきで目標値へ寄せるのが正しい摸しかたになる。


## 「間」の正体じゃ計算処理時間

- 変換表は 音声合成ROM にある(漢字 ROM とバンクを共有し、ポート 93h で切り替える)
- 展開先の作業領域は 0000h-3FFFh の 16KBで、画面の最上位ページと重なる。
  TALK 文を使うときに画面数を 2 以上にする必要があり、最大画面が使えなくなるのはこれが原因。
- 発声中は REQ ビットを監視し続けなければならず、
  所定の時間内に次のバイトを渡さないと ERR となる。BASIC は TALK 中に他の処理ができない。

キーボードを押すと途中で喋るのをやめてしまう
(`PRINT PEEK(&HFA5A) AND &H10` が 16 になる)のは、
サブ CPU の割り込みが REQ への応答を間に合わせなくするから。

実機にある話すまでの間は、Z80 が 16KB のワークエリアにフレーム列を組み立てていた実時間そのもの。
固定語だけ即座に喋るのは、チップ内蔵 ROM を番号で呼ぶだけでソフト処理がゼロだから。

## 出典

| | |
| --- | --- |
| [TALK文のできるまで](http://000.la.coocan.jp/p6/talk.html) | いちばん詳しい。実機のオーディオ出力波形を測って、AMP・ピッチ・F 値・B 値の具体的な振る舞いを割り出している。互換 BASIC を書いた過程の記録 |
| [PC-6001シリーズ技術情報いろいろ](https://000.la.coocan.jp/p6/tech.html) | ポート一覧とメモリマップ。音声合成用ポートの表 |
| [Ｐ６つくろうブログ 音声合成の設計(2)](http://sbeach.seesaa.net/article/387861494.html) | E0h〜E3h の仕様。ステータスの各ビットと手順 |
| [Ｐ６つくろうブログ 音声合成回路の設計(1)](http://sbeach.seesaa.net/article/387861426.html) | どの資料を当たったかの整理。5 段の 2 次 IIR という構成 |
| [cisc — μPD7752風味 音声合成エンジン](http://retropc.net/cisc/sound/) | チップのエミュレータ(C)。実装状況の表が、どこが未解明かの一覧にもなっている |
| [cisc — めも 2004-01](http://retropc.net/cisc/memo/0401/) | 「規則音声合成とはいっても、ほとんどの処理はソフトウェア側」の一節。分担の話はここから |
| [μPD7752エミュレータをJSに移植した](https://moriyoshi.hatenablog.com/entry/20090225/1235545774) | JS への移植。フォルマントパラメータが自由に使えないという指摘 |
| [N6x BASIC リファレンス 文法編1-19 音声合成](http://p6ers.net/hashi/bunpou1-19.html) | TALK 文の文法。作業領域と画面の重なりの記述 |
| [N6x BASIC リファレンス 応用編3章 声を出してみよう](http://p6ers.net/hashi/ouyou3.html) | TALK 文の解説。固定語、歌う機能、途中で止まる話 |

一次資料として上のページが引いているもの:

- 鈴木宗一ほか「ホルマントをパラメータとする音声合成LSI」, NEC技報, Vol.34, No.10 (1981/10), pp.92-97
- μPD7752 データシート
- 笠本正典「新・マシン語初級講座」, Oh!PC, Vol.3, No.8 (1985/1) — BASIC 側 TALK 文の解析
- えすび / Hashi「PC-Techknow6000 Vol.2」, TinyProject, 2015
